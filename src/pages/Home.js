import React from 'react';

export const Home = () => {
  return (
    <div
      style={{
        color: '#fff',
        fontSize: 48,
        fontWeight: 'bold',
        marginTop: 100,
      }}
    >
      Please select a region from the navbar
    </div>
  );
};
